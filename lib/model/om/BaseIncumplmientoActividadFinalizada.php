<?php


/**
 * Base class that represents a row from the 'incumplmiento_actividad_finalizada' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.6.2-dev on:
 *
 * Tue Feb 17 18:35:36 2015
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseIncumplmientoActividadFinalizada extends BaseObject  implements Persistent
{

	/**
	 * Peer class name
	 */
	const PEER = 'IncumplmientoActividadFinalizadaPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        IncumplmientoActividadFinalizadaPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        string
	 */
	protected $id;

	/**
	 * The value for the id_actividad field.
	 * @var        int
	 */
	protected $id_actividad;

	/**
	 * The value for the id_feria field.
	 * @var        int
	 */
	protected $id_feria;

	/**
	 * The value for the id_actividad_finalizada field.
	 * @var        int
	 */
	protected $id_actividad_finalizada;

	/**
	 * The value for the id_incumplimiento_actividad field.
	 * @var        int
	 */
	protected $id_incumplimiento_actividad;

	/**
	 * @var        Actividad
	 */
	protected $aActividad;

	/**
	 * @var        Feria
	 */
	protected $aFeria;

	/**
	 * @var        ActividadFinalizada
	 */
	protected $aActividadFinalizada;

	/**
	 * @var        IncumplimientoActividad
	 */
	protected $aIncumplimientoActividad;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Get the [id] column value.
	 * 
	 * @return     string
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [id_actividad] column value.
	 * 
	 * @return     int
	 */
	public function getIdActividad()
	{
		return $this->id_actividad;
	}

	/**
	 * Get the [id_feria] column value.
	 * 
	 * @return     int
	 */
	public function getIdFeria()
	{
		return $this->id_feria;
	}

	/**
	 * Get the [id_actividad_finalizada] column value.
	 * 
	 * @return     int
	 */
	public function getIdActividadFinalizada()
	{
		return $this->id_actividad_finalizada;
	}

	/**
	 * Get the [id_incumplimiento_actividad] column value.
	 * 
	 * @return     int
	 */
	public function getIdIncumplimientoActividad()
	{
		return $this->id_incumplimiento_actividad;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      string $v new value
	 * @return     IncumplmientoActividadFinalizada The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = IncumplmientoActividadFinalizadaPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [id_actividad] column.
	 * 
	 * @param      int $v new value
	 * @return     IncumplmientoActividadFinalizada The current object (for fluent API support)
	 */
	public function setIdActividad($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_actividad !== $v) {
			$this->id_actividad = $v;
			$this->modifiedColumns[] = IncumplmientoActividadFinalizadaPeer::ID_ACTIVIDAD;
		}

		if ($this->aActividad !== null && $this->aActividad->getId() !== $v) {
			$this->aActividad = null;
		}

		return $this;
	} // setIdActividad()

	/**
	 * Set the value of [id_feria] column.
	 * 
	 * @param      int $v new value
	 * @return     IncumplmientoActividadFinalizada The current object (for fluent API support)
	 */
	public function setIdFeria($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_feria !== $v) {
			$this->id_feria = $v;
			$this->modifiedColumns[] = IncumplmientoActividadFinalizadaPeer::ID_FERIA;
		}

		if ($this->aFeria !== null && $this->aFeria->getId() !== $v) {
			$this->aFeria = null;
		}

		return $this;
	} // setIdFeria()

	/**
	 * Set the value of [id_actividad_finalizada] column.
	 * 
	 * @param      int $v new value
	 * @return     IncumplmientoActividadFinalizada The current object (for fluent API support)
	 */
	public function setIdActividadFinalizada($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_actividad_finalizada !== $v) {
			$this->id_actividad_finalizada = $v;
			$this->modifiedColumns[] = IncumplmientoActividadFinalizadaPeer::ID_ACTIVIDAD_FINALIZADA;
		}

		if ($this->aActividadFinalizada !== null && $this->aActividadFinalizada->getId() !== $v) {
			$this->aActividadFinalizada = null;
		}

		return $this;
	} // setIdActividadFinalizada()

	/**
	 * Set the value of [id_incumplimiento_actividad] column.
	 * 
	 * @param      int $v new value
	 * @return     IncumplmientoActividadFinalizada The current object (for fluent API support)
	 */
	public function setIdIncumplimientoActividad($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_incumplimiento_actividad !== $v) {
			$this->id_incumplimiento_actividad = $v;
			$this->modifiedColumns[] = IncumplmientoActividadFinalizadaPeer::ID_INCUMPLIMIENTO_ACTIVIDAD;
		}

		if ($this->aIncumplimientoActividad !== null && $this->aIncumplimientoActividad->getId() !== $v) {
			$this->aIncumplimientoActividad = null;
		}

		return $this;
	} // setIdIncumplimientoActividad()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->id_actividad = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->id_feria = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
			$this->id_actividad_finalizada = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->id_incumplimiento_actividad = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			return $startcol + 5; // 5 = IncumplmientoActividadFinalizadaPeer::NUM_HYDRATE_COLUMNS.

		} catch (Exception $e) {
			throw new PropelException("Error populating IncumplmientoActividadFinalizada object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aActividad !== null && $this->id_actividad !== $this->aActividad->getId()) {
			$this->aActividad = null;
		}
		if ($this->aFeria !== null && $this->id_feria !== $this->aFeria->getId()) {
			$this->aFeria = null;
		}
		if ($this->aActividadFinalizada !== null && $this->id_actividad_finalizada !== $this->aActividadFinalizada->getId()) {
			$this->aActividadFinalizada = null;
		}
		if ($this->aIncumplimientoActividad !== null && $this->id_incumplimiento_actividad !== $this->aIncumplimientoActividad->getId()) {
			$this->aIncumplimientoActividad = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(IncumplmientoActividadFinalizadaPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = IncumplmientoActividadFinalizadaPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aActividad = null;
			$this->aFeria = null;
			$this->aActividadFinalizada = null;
			$this->aIncumplimientoActividad = null;
		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(IncumplmientoActividadFinalizadaPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}

		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseIncumplmientoActividadFinalizada:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			    return;
			  }
			}

			if ($ret) {
				IncumplmientoActividadFinalizadaQuery::create()
					->filterByPrimaryKey($this->getPrimaryKey())
					->delete($con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseIncumplmientoActividadFinalizada:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$con->commit();
				$this->setDeleted(true);
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(IncumplmientoActividadFinalizadaPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}

		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseIncumplmientoActividadFinalizada:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			  	$con->commit();
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseIncumplmientoActividadFinalizada:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				IncumplmientoActividadFinalizadaPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aActividad !== null) {
				if ($this->aActividad->isModified() || $this->aActividad->isNew()) {
					$affectedRows += $this->aActividad->save($con);
				}
				$this->setActividad($this->aActividad);
			}

			if ($this->aFeria !== null) {
				if ($this->aFeria->isModified() || $this->aFeria->isNew()) {
					$affectedRows += $this->aFeria->save($con);
				}
				$this->setFeria($this->aFeria);
			}

			if ($this->aActividadFinalizada !== null) {
				if ($this->aActividadFinalizada->isModified() || $this->aActividadFinalizada->isNew()) {
					$affectedRows += $this->aActividadFinalizada->save($con);
				}
				$this->setActividadFinalizada($this->aActividadFinalizada);
			}

			if ($this->aIncumplimientoActividad !== null) {
				if ($this->aIncumplimientoActividad->isModified() || $this->aIncumplimientoActividad->isNew()) {
					$affectedRows += $this->aIncumplimientoActividad->save($con);
				}
				$this->setIncumplimientoActividad($this->aIncumplimientoActividad);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = IncumplmientoActividadFinalizadaPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$criteria = $this->buildCriteria();
					if ($criteria->keyContainsValue(IncumplmientoActividadFinalizadaPeer::ID) ) {
						throw new PropelException('Cannot insert a value for auto-increment primary key ('.IncumplmientoActividadFinalizadaPeer::ID.')');
					}

					$pk = BasePeer::doInsert($criteria, $con);
					$affectedRows += 1;
					$this->setId($pk);  //[IMV] update autoincrement primary key
					$this->setNew(false);
				} else {
					$affectedRows += IncumplmientoActividadFinalizadaPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aActividad !== null) {
				if (!$this->aActividad->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aActividad->getValidationFailures());
				}
			}

			if ($this->aFeria !== null) {
				if (!$this->aFeria->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aFeria->getValidationFailures());
				}
			}

			if ($this->aActividadFinalizada !== null) {
				if (!$this->aActividadFinalizada->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aActividadFinalizada->getValidationFailures());
				}
			}

			if ($this->aIncumplimientoActividad !== null) {
				if (!$this->aIncumplimientoActividad->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aIncumplimientoActividad->getValidationFailures());
				}
			}


			if (($retval = IncumplmientoActividadFinalizadaPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}



			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = IncumplmientoActividadFinalizadaPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getIdActividad();
				break;
			case 2:
				return $this->getIdFeria();
				break;
			case 3:
				return $this->getIdActividadFinalizada();
				break;
			case 4:
				return $this->getIdIncumplimientoActividad();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 *                    Defaults to BasePeer::TYPE_PHPNAME.
	 * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to TRUE.
	 * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
	 * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
	 *
	 * @return    array an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
	{
		if (isset($alreadyDumpedObjects['IncumplmientoActividadFinalizada'][$this->getPrimaryKey()])) {
			return '*RECURSION*';
		}
		$alreadyDumpedObjects['IncumplmientoActividadFinalizada'][$this->getPrimaryKey()] = true;
		$keys = IncumplmientoActividadFinalizadaPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getIdActividad(),
			$keys[2] => $this->getIdFeria(),
			$keys[3] => $this->getIdActividadFinalizada(),
			$keys[4] => $this->getIdIncumplimientoActividad(),
		);
		if ($includeForeignObjects) {
			if (null !== $this->aActividad) {
				$result['Actividad'] = $this->aActividad->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
			}
			if (null !== $this->aFeria) {
				$result['Feria'] = $this->aFeria->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
			}
			if (null !== $this->aActividadFinalizada) {
				$result['ActividadFinalizada'] = $this->aActividadFinalizada->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
			}
			if (null !== $this->aIncumplimientoActividad) {
				$result['IncumplimientoActividad'] = $this->aIncumplimientoActividad->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
			}
		}
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = IncumplmientoActividadFinalizadaPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setIdActividad($value);
				break;
			case 2:
				$this->setIdFeria($value);
				break;
			case 3:
				$this->setIdActividadFinalizada($value);
				break;
			case 4:
				$this->setIdIncumplimientoActividad($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = IncumplmientoActividadFinalizadaPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setIdActividad($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setIdFeria($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setIdActividadFinalizada($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setIdIncumplimientoActividad($arr[$keys[4]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(IncumplmientoActividadFinalizadaPeer::DATABASE_NAME);

		if ($this->isColumnModified(IncumplmientoActividadFinalizadaPeer::ID)) $criteria->add(IncumplmientoActividadFinalizadaPeer::ID, $this->id);
		if ($this->isColumnModified(IncumplmientoActividadFinalizadaPeer::ID_ACTIVIDAD)) $criteria->add(IncumplmientoActividadFinalizadaPeer::ID_ACTIVIDAD, $this->id_actividad);
		if ($this->isColumnModified(IncumplmientoActividadFinalizadaPeer::ID_FERIA)) $criteria->add(IncumplmientoActividadFinalizadaPeer::ID_FERIA, $this->id_feria);
		if ($this->isColumnModified(IncumplmientoActividadFinalizadaPeer::ID_ACTIVIDAD_FINALIZADA)) $criteria->add(IncumplmientoActividadFinalizadaPeer::ID_ACTIVIDAD_FINALIZADA, $this->id_actividad_finalizada);
		if ($this->isColumnModified(IncumplmientoActividadFinalizadaPeer::ID_INCUMPLIMIENTO_ACTIVIDAD)) $criteria->add(IncumplmientoActividadFinalizadaPeer::ID_INCUMPLIMIENTO_ACTIVIDAD, $this->id_incumplimiento_actividad);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(IncumplmientoActividadFinalizadaPeer::DATABASE_NAME);
		$criteria->add(IncumplmientoActividadFinalizadaPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     string
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      string $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Returns true if the primary key for this object is null.
	 * @return     boolean
	 */
	public function isPrimaryKeyNull()
	{
		return null === $this->getId();
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of IncumplmientoActividadFinalizada (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @param      boolean $makeNew Whether to reset autoincrement PKs and make the object new.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
	{
		$copyObj->setIdActividad($this->getIdActividad());
		$copyObj->setIdFeria($this->getIdFeria());
		$copyObj->setIdActividadFinalizada($this->getIdActividadFinalizada());
		$copyObj->setIdIncumplimientoActividad($this->getIdIncumplimientoActividad());
		if ($makeNew) {
			$copyObj->setNew(true);
			$copyObj->setId(NULL); // this is a auto-increment column, so set to default value
		}
	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     IncumplmientoActividadFinalizada Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     IncumplmientoActividadFinalizadaPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new IncumplmientoActividadFinalizadaPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Actividad object.
	 *
	 * @param      Actividad $v
	 * @return     IncumplmientoActividadFinalizada The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setActividad(Actividad $v = null)
	{
		if ($v === null) {
			$this->setIdActividad(NULL);
		} else {
			$this->setIdActividad($v->getId());
		}

		$this->aActividad = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Actividad object, it will not be re-added.
		if ($v !== null) {
			$v->addIncumplmientoActividadFinalizada($this);
		}

		return $this;
	}


	/**
	 * Get the associated Actividad object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Actividad The associated Actividad object.
	 * @throws     PropelException
	 */
	public function getActividad(PropelPDO $con = null)
	{
		if ($this->aActividad === null && ($this->id_actividad !== null)) {
			$this->aActividad = ActividadQuery::create()->findPk($this->id_actividad, $con);
			/* The following can be used additionally to
				guarantee the related object contains a reference
				to this object.  This level of coupling may, however, be
				undesirable since it could result in an only partially populated collection
				in the referenced object.
				$this->aActividad->addIncumplmientoActividadFinalizadas($this);
			 */
		}
		return $this->aActividad;
	}

	/**
	 * Declares an association between this object and a Feria object.
	 *
	 * @param      Feria $v
	 * @return     IncumplmientoActividadFinalizada The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setFeria(Feria $v = null)
	{
		if ($v === null) {
			$this->setIdFeria(NULL);
		} else {
			$this->setIdFeria($v->getId());
		}

		$this->aFeria = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Feria object, it will not be re-added.
		if ($v !== null) {
			$v->addIncumplmientoActividadFinalizada($this);
		}

		return $this;
	}


	/**
	 * Get the associated Feria object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Feria The associated Feria object.
	 * @throws     PropelException
	 */
	public function getFeria(PropelPDO $con = null)
	{
		if ($this->aFeria === null && ($this->id_feria !== null)) {
			$this->aFeria = FeriaQuery::create()->findPk($this->id_feria, $con);
			/* The following can be used additionally to
				guarantee the related object contains a reference
				to this object.  This level of coupling may, however, be
				undesirable since it could result in an only partially populated collection
				in the referenced object.
				$this->aFeria->addIncumplmientoActividadFinalizadas($this);
			 */
		}
		return $this->aFeria;
	}

	/**
	 * Declares an association between this object and a ActividadFinalizada object.
	 *
	 * @param      ActividadFinalizada $v
	 * @return     IncumplmientoActividadFinalizada The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setActividadFinalizada(ActividadFinalizada $v = null)
	{
		if ($v === null) {
			$this->setIdActividadFinalizada(NULL);
		} else {
			$this->setIdActividadFinalizada($v->getId());
		}

		$this->aActividadFinalizada = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the ActividadFinalizada object, it will not be re-added.
		if ($v !== null) {
			$v->addIncumplmientoActividadFinalizada($this);
		}

		return $this;
	}


	/**
	 * Get the associated ActividadFinalizada object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     ActividadFinalizada The associated ActividadFinalizada object.
	 * @throws     PropelException
	 */
	public function getActividadFinalizada(PropelPDO $con = null)
	{
		if ($this->aActividadFinalizada === null && ($this->id_actividad_finalizada !== null)) {
			$this->aActividadFinalizada = ActividadFinalizadaQuery::create()->findPk($this->id_actividad_finalizada, $con);
			/* The following can be used additionally to
				guarantee the related object contains a reference
				to this object.  This level of coupling may, however, be
				undesirable since it could result in an only partially populated collection
				in the referenced object.
				$this->aActividadFinalizada->addIncumplmientoActividadFinalizadas($this);
			 */
		}
		return $this->aActividadFinalizada;
	}

	/**
	 * Declares an association between this object and a IncumplimientoActividad object.
	 *
	 * @param      IncumplimientoActividad $v
	 * @return     IncumplmientoActividadFinalizada The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setIncumplimientoActividad(IncumplimientoActividad $v = null)
	{
		if ($v === null) {
			$this->setIdIncumplimientoActividad(NULL);
		} else {
			$this->setIdIncumplimientoActividad($v->getId());
		}

		$this->aIncumplimientoActividad = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the IncumplimientoActividad object, it will not be re-added.
		if ($v !== null) {
			$v->addIncumplmientoActividadFinalizada($this);
		}

		return $this;
	}


	/**
	 * Get the associated IncumplimientoActividad object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     IncumplimientoActividad The associated IncumplimientoActividad object.
	 * @throws     PropelException
	 */
	public function getIncumplimientoActividad(PropelPDO $con = null)
	{
		if ($this->aIncumplimientoActividad === null && ($this->id_incumplimiento_actividad !== null)) {
			$this->aIncumplimientoActividad = IncumplimientoActividadQuery::create()->findPk($this->id_incumplimiento_actividad, $con);
			/* The following can be used additionally to
				guarantee the related object contains a reference
				to this object.  This level of coupling may, however, be
				undesirable since it could result in an only partially populated collection
				in the referenced object.
				$this->aIncumplimientoActividad->addIncumplmientoActividadFinalizadas($this);
			 */
		}
		return $this->aIncumplimientoActividad;
	}

	/**
	 * Clears the current object and sets all attributes to their default values
	 */
	public function clear()
	{
		$this->id = null;
		$this->id_actividad = null;
		$this->id_feria = null;
		$this->id_actividad_finalizada = null;
		$this->id_incumplimiento_actividad = null;
		$this->alreadyInSave = false;
		$this->alreadyInValidation = false;
		$this->clearAllReferences();
		$this->resetModified();
		$this->setNew(true);
		$this->setDeleted(false);
	}

	/**
	 * Resets all references to other model objects or collections of model objects.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect
	 * objects with circular references (even in PHP 5.3). This is currently necessary
	 * when using Propel in certain daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all referrer objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
		} // if ($deep)

		$this->aActividad = null;
		$this->aFeria = null;
		$this->aActividadFinalizada = null;
		$this->aIncumplimientoActividad = null;
	}

	/**
	 * Return the string representation of this object
	 *
	 * @return string
	 */
	public function __toString()
	{
		return (string) $this->exportTo(IncumplmientoActividadFinalizadaPeer::DEFAULT_STRING_FORMAT);
	}

	/**
	 * Catches calls to virtual methods
	 */
	public function __call($name, $params)
	{
		// symfony_behaviors behavior
		if ($callable = sfMixer::getCallable('BaseIncumplmientoActividadFinalizada:' . $name))
		{
		  array_unshift($params, $this);
		  return call_user_func_array($callable, $params);
		}

		if (preg_match('/get(\w+)/', $name, $matches)) {
			$virtualColumn = $matches[1];
			if ($this->hasVirtualColumn($virtualColumn)) {
				return $this->getVirtualColumn($virtualColumn);
			}
			// no lcfirst in php<5.3...
			$virtualColumn[0] = strtolower($virtualColumn[0]);
			if ($this->hasVirtualColumn($virtualColumn)) {
				return $this->getVirtualColumn($virtualColumn);
			}
		}
		return parent::__call($name, $params);
	}

} // BaseIncumplmientoActividadFinalizada
